kind: ConfigMap
apiVersion: v1
metadata:
  name: alertmanager-config
  namespace: monitoring
data:
  config.yml: |-
    global:
    templates:
    - '/etc/alertmanager/*.tmpl'
    route:
      receiver: alert-emailer
      group_by: ['alertname', 'priority']
      group_wait: 10s
      repeat_interval: 30m

    receivers:
    - name: alert-emailer
      email_configs:
      - to: temp88t@gmail.com
        send_resolved: false
        from: prometheus-alert@email.com
        smarthost: smtp.gmail.com:465
        require_tls: false
        auth_username: temp88t@gmail.com
        auth_password: iskmeivlshqprnqq
